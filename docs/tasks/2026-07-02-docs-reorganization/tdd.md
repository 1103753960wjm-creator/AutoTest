# TDD：docs 文档目录整理

## 1. 标题

- 任务名称：docs 文档目录整理
- 日期：2026-07-02
- 关联 Spec/SDD：`docs/tasks/2026-07-02-docs-reorganization/spec-sdd.md`
- 当前阶段：TDD
- 目标验证等级：V2

## 2. 大白话测试目标

- 证明 `docs` 根目录已经变清爽。
- 证明文档分类后还能找到主要文档。
- 证明旧路径引用没有明显残留。
- 证明规则检查脚本仍然可执行。

## 3. 测试范围

- 必测模块：`docs`
- 必测页面：无
- 必测接口：无
- 必测数据或状态：无
- 不测范围及原因：不测前后端页面，因为本轮没有改业务代码。

## 4. 测试数据准备

- 需要账号或权限：无
- 需要项目数据：无
- 正常数据：当前 `docs` 文档集合
- 异常数据：旧路径引用、缺失文档引用
- 是否需要旧数据兼容样本：需要，检查历史文档中的 docs 路径引用

## 5. 主流程用例

| 编号 | 操作步骤 | 预期结果 | 通过标准 |
| --- | --- | --- | --- |
| TC-01 | 查看 `docs` 根目录 Markdown 文件 | 根目录只保留入口 README | 无大量散落 `.md` |
| TC-02 | 查看 `docs` 子目录 | 有按主题分类的目录 | 能看到 `architecture`、`api-automation`、`data-factory` 等目录 |
| TC-03 | 查看 `docs/README.md` | 能说明每个目录用途 | 分类说明清楚 |

## 6. 异常和边界用例

| 编号 | 场景 | 操作步骤 | 预期结果 | 通过标准 |
| --- | --- | --- | --- | --- |
| EX-01 | 旧路径残留 | 搜索典型旧路径 | 不再命中旧根路径 | `rg` 无旧路径残留 |
| EX-02 | 缺失文档引用 | 扫描 docs 路径引用 | 不存在明显缺失路径 | 路径存在性检查通过 |
| EX-03 | 规则脚本执行 | 执行 `scripts/rule_check.ps1` | 规则检查通过 | 返回 0 |

## 7. 回归检查

- 受影响旧入口：docs 根目录下的旧 Markdown 路径
- 受影响旧字段：无
- 受影响旧页面：无
- 受影响缓存、轮询、异步任务、日志或通知：无

## 8. 验证命令和页面操作

- 根目录检查：`Get-ChildItem -LiteralPath docs -File -Filter *.md`
- 旧路径残留检查：用 `rg` 扫描典型旧路径模式
- 文档路径存在性检查：PowerShell 扫描 docs 路径引用并判断文件是否存在
- 规则检查：`powershell -ExecutionPolicy Bypass -File scripts/rule_check.ps1`
- 空白检查：扫描尾随空白
- diff 检查：`git diff --check`

## 9. 失败判定

- `docs` 根目录仍有大量业务 Markdown，算失败。
- 典型旧路径仍残留，算失败。
- 文档路径存在性检查发现缺失路径，算失败。
- 规则检查脚本失败，算失败。
- 失败后允许修复范围：只允许修 `docs`、`.cursor`、`AGENTS.md`、`更新日志.md`。
- 失败后必须写入：`docs/project-memory/error_event_log.md`。

## 10. 进入 Execution 条件

- [x] 主流程用例已明确
- [x] 异常和边界用例已明确
- [x] 验证命令已明确
- [x] 失败判定已明确
- [x] 用户已确认可以进入实现
