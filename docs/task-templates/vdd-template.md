# VDD 模板

## 1. 标题

- 任务名称：
- 日期：
- 关联 Spec/SDD：
- 关联 TDD：
- 当前阶段：VDD
- 本轮最高验证等级：V0 / V1 / V2 / V3 / V4

## 2. 本轮改了什么

- 大白话总结：
- 修改的主要文件：
- 新增的主要文件：
- 没有改但容易被误解的范围：

## 3. 验收标准核对

| 验收项 | 是否通过 | 证据 | 备注 |
| --- | --- | --- | --- |
| 用户可见行为 1 | 是 / 否 / 未验证 |  |  |
| 用户可见行为 2 | 是 / 否 / 未验证 |  |  |
| 异常场景 | 是 / 否 / 未验证 |  |  |
| 旧入口或旧数据兼容 | 是 / 否 / 未验证 |  |  |
| 不引入明显回归 | 是 / 否 / 未验证 |  |  |

## 4. 验证执行记录

| 验证类型 | 命令或操作 | 结果 | 证据 |
| --- | --- | --- | --- |
| 静态验证 |  | 通过 / 失败 / 未执行 |  |
| 构建验证 |  | 通过 / 失败 / 未执行 |  |
| 接口验证 |  | 通过 / 失败 / 未执行 |  |
| 页面验证 |  | 通过 / 失败 / 未执行 |  |
| 规则检查 | `powershell -ExecutionPolicy Bypass -File scripts/rule_check.ps1` | 通过 / 失败 / 未执行 |  |

## 5. 失败和错误事件

- 本轮是否出现错误事件：是 / 否
- 已写入 `error_event_log.md` 的事件：
- 是否升级到 `error_prevention_log.md`：是 / 否 / 待判断
- 未解决错误：

## 6. 未验证项

- 未验证项 1：
- 未验证原因：
- 可能风险：
- 后续如何补验证：

## 7. 残余风险

- 风险 1：
- 风险 2：
- 用户需要知道的限制：

## 8. 回退和止损

- 可以直接回退的文件：
- 不可直接回退的变更：
- 回退后需要验证什么：
- 是否涉及数据修复或迁移回滚：

## 9. 最终结论

- 是否达到本轮目标：是 / 否 / 部分达到
- 是否可以交付：是 / 否
- 需要用户继续确认的事项：
- 下一步建议：
