# Loop Contract 模板

## 1. 本轮目标

- 大白话说明本轮最终要达到什么效果：
- 用户能看到或验证到的结果：

## 2. 本轮不做

- 不做事项 1：
- 不做事项 2：
- 明确不碰的模块或链路：

## 3. 自治等级

- 本轮自治等级：L1 / L2 / L3
- 判断理由：
- 是否允许 Loop：是 / 否

## 4. 允许 AI 自主决定

- 文件搜索和代码阅读：
- 局部实现细节：
- 小范围兼容修正：
- 最小验证命令：

## 5. 必须暂停询问

- 接口字段变化：
- 返回结构变化：
- 数据模型或迁移：
- 新增依赖：
- 删除、清空或不可逆操作：
- 高风险链路变化：
- 验证连续失败 2 次：
- 发现规则冲突：

## 6. 变更预算

- 最多修改文件数：
- 允许修改目录：
- 禁止修改目录：
- 是否允许改接口：是 / 否
- 是否允许改模型：是 / 否
- 是否允许改测试：是 / 否
- 是否允许新增依赖：是 / 否
- 是否允许改规则文件：是 / 否

## 7. 验证目标

- 目标验证等级：V1 / V2 / V3 / V4
- 必须执行的验证：
- 主流程验证：
- 异常流程验证：
- 旧入口或旧数据兼容验证：

## 8. 失败处理

- 失败写入位置：`docs/project-memory/error_event_log.md`
- 同一失败最多允许重试次数：
- 失败后允许修复范围：
- 失败后必须暂停的条件：

## 9. 回退与止损

- 可直接回退的文件：
- 不可直接回退的变更：
- 是否涉及数据变更：
- 是否涉及迁移：
- 是否需要 feature flag：
- 回退后如何验证：

## 10. 停止条件

- 验证通过：
- 达到最大循环次数：
- 连续失败：
- 环境阻塞：
- 触发高风险：
- 用户需要重新确认：
