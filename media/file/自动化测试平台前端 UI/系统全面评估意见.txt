这个项目现在已经具备“平台雏形”，但还没有完成从“功能集合”到“工作流平台”的升级。
也就是说，它现在最大的问题不是“没有功能”，而是：

功能入口很多，但信息架构还不够稳

核心工作流已经串起来了，但闭环还不够完整

页面已经能用，但还不是 2026 年那种“低学习成本、高连贯性”的工作台

代码已经能跑，但继续堆功能会越来越难维护

你现在最适合的路线，不是“全部重写”，而是走：

底座止血 → 导航和体验统一 → 测试设计闭环补全 → 自动化三大工作台重构 → 云真机/任务/结果联动 → AI 治理化

下面我按你要的模块来拆，而且我会同时讲：

当前问题

该怎么优化 UI/UX

前端怎么改

后端怎么补闭环

这块排第几阶段做最合适

一、我先说整体诊断

你这个版本比上一版明显成熟了，尤其是：

测试设计 已经不是空菜单，而是有 需求资产 / 测试用例 / 用例生成 / 自动化草稿确认

后端有 requirement / testcase / automation_draft / ai

docs/specs 已经把 M2、M3、M4 的方向写出来了

API / Web / App 页面也开始支持通过 query 自动定位自动化草稿生成的目标资产

这说明方向是对的。

但现在平台里有 4 个明显的“结构性问题”：

1. 信息架构还混着“模板后台”和“测试平台”

比如主控台还留着明显模板残留；系统管理有一套 user_view/*，同时前端里又保留了 views/system/* 的另一套后台模板页面；顶部快捷标签也还是“页面堆叠式”。

2. 核心链路已经有了，但“闭环感”还不够强

你现在其实已经接近这条主线：

需求 → 测试用例 → 自动化草稿 → API/Web/App 资产 → 任务 → 结果

但还缺：

版本

去重

评审留痕

结果反向追溯

AI 调用治理

设备/任务/结果之间的统一入口

3. 前端已经出现“巨石页面”

我看到几个核心页面还是特别大：

web.vue 2550 行

api_project.vue 1769 行

app.vue 1682 行

app_run.vue 1578 行

再加上大量 any 和 console.log，说明现在已经进入“每加一点功能都会更难改”的阶段。

4. 有几块底层风险必须先止血

这几项要优先处理：

权限菜单还是本地 authMenu.json 驱动，不是真正后端权限

auth.ts getter 里 buttonList / buttons 命名不一致

storage.ts 里 window.localStorage.put/get 这种写法本身就不对

axios 还是把 token/user_id 往 body 里塞

登录还是前端 MD5，后端直接按值查库

eval、shell=True 还在

路由里有重复路径，/app_report 出现了两次，/app_auto 也有父子同路径

.env 和配置里还有很多硬编码 IP

首启初始化里 Role.create(role_id=1, user_id=1) 和当前模型字段看起来不一致，这个很像首启风险点

所以这次我给你的方案会分两层：

先保命，再升级。

二、模块级详细优化建议
1）主控台
当前状态

现在主控台的结构还是典型后台模板改造：

欢迎卡片

四个统计卡

执行统计图

下面还留了“地区异常订单排行”“近10日订单量”这类非测试业务图表

这个页面的问题不在“丑”，而在于不聚焦。
测试平台的首页不该只是“展示”，而应该是“进入工作”。

你应该把它改成什么

2026 年更合理的主控台应该是一个测试工作台，而不是“图表大屏”。

建议改成 4 个区域：

第一屏：今日状态

今日运行数

失败数

通过率

空闲设备数

待处理告警

待审核用例数

第二屏：我的工作

我的待处理测试设计

我的最近失败任务

我的最近使用资产

我的常用入口

第三屏：风险看板

最近 24 小时失败 Top 任务

最近失败最多的 API/Web/App 资产

最近 AI 生成失败/保存失败次数

第四屏：快捷继续

继续编辑最近的自动化草稿

继续上次打开的 API/Web/App 资产

最近查看的测试用例/需求

前端怎么改

把首页从“图表卡片页”改成“工作卡片页”

顶部快捷标签不要默认铺满所有页面，改成“最近访问 + 收藏入口”

卡片点击直接跳对应详情，不要只是展示数字

所有统计卡支持 drill-down，一点就能筛到对应列表

后端怎么补

新增一个真正的 dashboard 聚合接口，而不是页面自己拼多个接口：

/api/dashboard/overview

/api/dashboard/my_work

/api/dashboard/recent_failures

/api/dashboard/quick_entries

这一块优先级

P0，第一阶段就做。
因为它最容易直接提升“顺手感”。

2）实用工具
当前子模块

JSON解析

在线加密解密

时间戳转换

当前问题

这些工具能用，但还是“演示页逻辑”：

视觉密度低

没有统一工具壳

没有收藏/最近使用

没有复制成功反馈和快捷输入输出

工具之间缺少搜索和入口整理

2026 年更合理的做法

把它做成一个轻量的工具中心，而不是三个孤立页面。

建议加的能力

不需要一口气全做，优先做这些最贴合测试工作：

JSON 格式化 / 压缩 / 校验

URL/Base64/Unicode 编解码

时间戳转换

Curl 转接口请求

正则测试

Cron 表达式解释

Header / Cookie / JWT 查看

JSONPath/XPath 小测试工具

UI/UX 怎么改

左侧工具列表，右侧工具工作区

顶部加搜索框

支持“最近使用”“收藏”

输入区和输出区并排

一键复制、一键清空、一键交换输入输出

错误提示直接显示在编辑区下方，不要只弹 toast

前端怎么改

抽一个统一 ToolShell 组件

每个工具做成独立 composable

不再每页都手写同类按钮布局

后端怎么改

大部分工具可以纯前端，不必后端化。
后端只保留未来可能需要服务端能力的：

Curl 导入转接口资产

文件解析类

批量数据转换类

优先级

P2。
它对“锦上添花”帮助大，但不是当前平台闭环的核心。

3）云真机
当前子模块

从代码和截图看，云真机实际上已经不只是一个列表页，它包含：

设备列表

设备筛选

设备占用

设备详情弹窗

安装 APP

设备终端

文件列表

设备抓包

设备性能

使用记录

mitmproxy 相关日志

当前问题

这块功能很多，但仍然有两个典型问题：

第一，入口过散。
设备列表、设备详情、设备占用、抓包、安装、性能都堆在一个弹窗体系里。

第二，设备状态模型还不够稳。
从产品体验看，你需要的是“设备租约”，而不只是“设备是否空闲”。

2026 年更合理的云真机体验

云真机应该是一个设备工作台：

卡片墙只负责“找设备”

进入设备后是一个“会话页”

所有安装、抓包、性能、日志、文件都归到“当前会话”

明确显示“谁在用、何时到期、剩余会话时长”

UI/UX 怎么改
设备列表页

顶部改成状态 Tabs：全部 / 空闲 / 使用中 / 离线

品牌、系统版本改成多选 chips

搜索支持设备名 / 型号 / 使用人 / 设备号

卡片上直接显示：状态、品牌、系统版本、占用人、最近心跳

设备会话页

不要再做成“大弹窗里塞所有东西”，建议升级成标准工作区：

左侧：

设备实时画面

右侧：

会话信息

快捷操作

选项卡：安装 / 文件 / 终端 / 性能 / 抓包 / 历史

底部：

固定操作栏：停止使用 / 截图 / 重启会话 / 刷新

后端怎么补闭环

建议补这几个核心概念：

device_session：设备会话表

lease_expire_time：占用过期时间

heartbeat_time：心跳时间

session_status：申请中 / 使用中 / 释放中 / 异常中断

artifact：安装包、日志、抓包文件、性能文件统一归档

必须补的后端逻辑

设备占用做成租约，不只是开关状态

异常断开自动回收

安装/抓包/性能采集改异步任务

设备历史按 session 聚合，不要散在多个表里

优先级

P1。
因为这块已经很有价值，但还没真正形成“设备会话闭环”。

4）接口自动化
当前子模块

接口管理

API-场景管理

API-结果

当前问题

接口自动化目前的核心页面已经很强，但问题非常典型：

左树 + 中编辑 + 下响应 全塞一页

功能太强但信息层次不够清楚

场景、单接口、结果、环境、前后置、断言耦合太深

对测试人员来说很强，但新手学习成本偏高

2026 年更合理的接口工作台

建议改成真正的三层工作区：

左侧：资产导航

项目

服务

文件夹

接口

场景

中间：编辑区

基础信息

请求构造

参数

Header

Body

变量

断言

前后置动作

右侧/底部：执行区

响应

Header

断言结果

环境变量变更

前后置执行结果

关键 UI 优化

保存按钮固定

发送请求按钮固定

接口切换后记住上次打开 tab

支持最近打开接口

支持收藏接口

响应区支持格式化、复制、差异对比

断言失败直接高亮失败字段

前端代码优化

这个模块建议优先拆 api_project.vue：

拆成：

ApiTreePanel

ApiEditorHeader

ApiRequestTabs

ApiRuntimePanel

ApiAssertPanel

useApiWorkspace

后端优化

单接口资产和场景资产统一成可追踪对象

运行结果要能回链到：接口资产 / 场景资产 / 任务 / 用例 / 需求

环境快照执行时固化，不要运行后取当前环境

导入导出支持 OpenAPI/Postman（哪怕先只支持一部分）

逻辑闭环

要形成：

测试用例 → 自动化草稿 → API资产 → 任务 → 结果 → 回到用例

你现在已经做到“草稿能跳 API 资产”，下一步要做到“结果也能回链测试用例”。

优先级

P1。

5）WEB管理
当前子模块

元素管理

Web 自动化

Web-场景管理

Web结果

当前问题

这个模块是你平台最像“专业自动化工具”的一块，但也是代码风险最高的一块：

页面特别大

步骤编辑、运行、脚本列表、菜单树全耦在一个页面

执行器里还有 eval

UI 功能很多，但认知负担也最大

2026 年更合理的 Web 自动化体验

建议把 Web 自动化页改成脚本工作台：

左侧：

文件夹 / 脚本树

中间：

步骤列表

右侧：

当前步骤编辑器

底部：

调试结果 / 运行日志 / Trace / 截图

操作体验重点

步骤列表只显示关键信息：编号、名称、动作、状态

点击步骤，在右侧编辑，不要在列表里塞所有表单

动作类型做成“操作面板”，支持搜索和收藏

提供步骤模板：点击、输入、等待、断言、上传、截图、滚动

支持“从当前步骤开始调试”

支持“仅看失败步骤”

结果页增加日志搜索、失败定位、trace 快捷打开

元素管理建议

元素管理不要只是“元素库”，要升级成：

元素命名规范

定位器测试

失效检查

页面分组

最近失败元素

后端必须优先改

这块 P0 级别要做的不是 UI，而是执行安全：

把 eval 收掉

脚本行为改成白名单 DSL

自定义代码执行做沙箱

断言和动作统一解释器

优先级

P1（UI 工作台重构） + P0（执行安全）

6）APP管理
当前子模块

包体管理

APP自动化

图像库

APP结果

当前问题

APP 模块和 Web 模块一样，也开始出现“巨石工作台”问题：

包体、脚本、运行、结果、图像资产之间关联弱

编辑页和运行页耦合太深

结果回看能力还不够像“排障页”

图像库是独立页，但还没有成为真正的步骤资产

2026 年更合理的 APP 工作台
包体管理

包名、版本、渠道、上传人、上传时间、兼容平台

支持“标记默认包体”

支持和任务绑定固定版本

APP自动化

左树：菜单/脚本

中区：步骤流

右侧：步骤编辑器

底部：运行结果 / 截图 / 视频 / 性能

图像库

不只是图片列表

要支持“被哪些步骤引用”

支持图片相似查重

支持按页面、模块、终端分组

APP结果

失败步骤快速过滤

截图与步骤联动

结果页直接跳回脚本定位对应步骤

后端怎么补

包体、图像、脚本、结果统一资产 ID

运行时生成执行快照，不吃当前线上编辑内容

图像资产增加引用关系

结果表增加回链：script_id / menu_id / task_id / testcase_id

优先级

P1。

7）任务管理
当前子模块

消息通知

定时任务

当前问题

当前任务管理更像“技术配置页”，不是“测试任务中心”。

你作为测试工程师真正想看到的是：

有哪些任务

每个任务跑什么

下次什么时候跑

最近一次结果怎么样

失败了通知给谁

它对应哪个测试资产

2026 年更合理的任务中心

任务创建要变成向导式：

第一步：选资产

API 场景 / Web 场景 / App 脚本

第二步：选环境

浏览器 / 设备 / 接口环境 / 包体版本

第三步：选调度

单次 / 间隔 / 每日 / 每周

第四步：选通知

通知人 / 通知渠道 / 失败阈值

第五步：生成任务摘要

UI/UX 优化

任务列表展示最近运行结果和下一次执行时间

任务详情页展示执行历史

暂停/启用/立即执行分开

调度表达尽量自然语言化，不要全靠 cron 概念

后端优化

调度器任务状态要和数据库状态强一致

修改任务前先移除旧任务再注册新任务

增加任务执行历史表

增加任务快照，记录当时使用的资产版本/环境版本

通知失败要有重试和发送记录

优先级

P1。

8）测试设计
当前子模块

需求资产

测试用例

用例生成

自动化草稿确认

这是你现在最值得继续投入的一块

这块已经不是“附属功能”了，而是在决定这个平台后面是：

执行工具
还是
测试资产平台

当前做得好的地方

已经有 M2/M3 文档和测试设计文档

需求、测试用例、自动化草稿都独立成模块了

用例生成页已经是网格编辑思路

自动化草稿确认页也形成了“生成-确认-保存-跳转”链路

当前还缺的核心闭环
1. 需求资产与用例资产仍缺“更新策略”

现在更像每次保存都会新增一份，而不是基于已有资产更新。

你后面会很快遇到：

同标题需求越来越多

同一需求变更多个版本混在一起

无法判断哪个是当前有效资产

2. 测试用例资产更像“可查询”，还不是“可维护”

现在有列表、筛选、审核状态、自动化摘要，但还缺：

真正编辑

批量编辑

审核意见

驳回原因

版本对比

变更记录

3. 追踪关系还没完全打通

最理想的设计应该是：

需求 → 测试用例 → 自动化草稿 → API/Web/App 资产 → 任务 → 结果

再往后最好加：

缺陷链接

AI 调用日志

UI/UX 应该怎么升级
需求资产页

左侧需求列表，右侧详情+关联用例

支持覆盖率视图：该需求已生成多少用例、已审核多少、已转自动化多少

支持“从这里继续生成/继续修订”

测试用例页

继续保留 vxe-table

但不要只有审核状态维护，要补：

单条编辑

批量改优先级

批量改模块

审核意见

最近修改人

自动化进度摘要

用例生成页

很好，继续保留网格模式

但要增加“保存为新版本 / 更新已有需求”

增加重复识别

增加未保存变更提醒

自动化草稿确认页

当前已经很好，是个可继续打磨的点

建议增加：

差异对比

JSON 结构校验

占位字段高亮

自动回链到来源测试用例

保存前检查项

后端建议

这块优先补 5 件事：

需求唯一性和版本策略

用例唯一性和更新策略

审核意见、审核人、审核时间

资产变更历史

执行结果回链测试用例和需求

优先级

P0/P1 混合。
这块是平台的增长引擎，值得尽快做完闭环。

9）系统管理
当前子模块

角色管理

用户中心

登录日志

操作日志

AI配置

当前最大问题

系统管理现在是两套系统并存：

一套是你当前实际业务使用的 user_view/*

一套是 KOI 模板遗留的 views/system/* 和 api/system/*

这会导致：

代码体量变大

认知混乱

很多页面/API 实际没走到生产链路

后续 AI 继续写代码时更容易写到错误目录

必须做的第一件事

统一系统管理的唯一真源。

你要决定：

保留 user_view/* 并逐步扩展
还是

迁移到 views/system/*

我建议：
不要现在大迁移。先把 user_view/* 作为生产主线，views/system/* 标记为模板遗留并逐步清退。

角色/权限建议

当前权限不能再靠本地菜单模拟：

菜单由后端下发

按钮权限由后端下发

前端只渲染

接口权限校验统一放后端

AI配置建议

你现在已经有 AI 配置页了，但从 docs/specs 看，下一步最应该做的是：

Prompt 模板管理

AI 调用日志

AI 调用失败审计

AI 调用统计

也就是把 AI配置 从“设置页”升级成“AI治理中心”。

系统管理优先级

P0（权限、登录、鉴权、安全）
P2（AI治理视图）

三、跨模块必须统一做的 4 件大事
1）前端代码结构统一

建议别全仓大迁移，采用新模块新结构、旧模块逐步迁移。

建议的新结构

每个域模块按这个方式拆：

pages

components

composables

api

types

constants

先从这几个模块开始示范：

test_design

automation_draft

dashboard

device

当前必须先修的前端问题

auth.ts 的 getter 命名错误

storage.ts 的错误实现

重复路由

本地菜单权限

大量 any

大量 console.log

页面状态不持久

2）后端分层

当前后端很多逻辑还在 view.py 和 *_common.py 里堆着。

建议按“渐进式重构”来：

第一层

router：收参数、回响应

第二层

service：业务流程

第三层

repository：数据库操作

第四层

schema：请求响应模型

先不要全仓迁移，优先用于：

requirement

testcase

automation_draft

ai

task

3）安全和鉴权

这个必须排最高优先级。

先做这几项

token 改 header 传递

后端密码哈希存储

登录限流

后端菜单权限下发

危险执行逻辑收口

统一异常返回结构

4）UI/UX 设计系统

你现在已经有 KoiCard / KoiDialog / KoiDrawer / KoiToolbar 这些自定义组件了，这很好。

不要推翻，应该做的是：

升级这套组件

卡片头部统一

搜索栏统一

表格页统一

详情抽屉统一

结果页统一

状态标签统一

空态 / 加载态 / 失败态统一

这样你后面让 AI 改页面时，能显著减少“每页长得都不一样”的问题。

四、你这套平台真正需要补齐的“逻辑闭环”

这是我最想强调的一部分。

1）测试设计闭环

需求 → 测试用例 → 自动化草稿 → 自动化资产

当前已经 70% 了，还缺：

更新策略

版本

审核留痕

去重

变更历史

2）执行闭环

自动化资产 → 任务 → 执行结果 → 失败定位 → 回到资产

你现在还需要：

结果回链资产

结果回链任务

结果回链测试用例

失败定位快捷入口

3）设备闭环

设备 → 会话 → 安装/抓包/性能 → 使用记录 → 释放

你现在要补：

会话概念

租约

心跳

自动回收

会话级归档

4）治理闭环

AI配置 → Prompt模板 → 调用日志 → 失败审计 → 统计

这部分 docs 已经写到 M4 了，方向非常对，建议按文档推进。

五、最适合你现在的分阶段规划

你说得很对，直接全改肯定不行。
所以我建议分 6 个阶段，每个阶段都是“可独立交付”的。

阶段 0：底座止血

目标：先把明显坑补掉，不改大 UI。

做什么

修 storage.ts

修 auth.ts

去掉本地假动态权限，至少先接后端菜单接口

token 改 header

登录/密码改后端哈希

清理重复路由

清理硬编码 IP

建 .env.example

补 Docker Compose 或最少一键启动文档

跑通现有 tests，补 CI 最小链路

验收

登录不回归

菜单正常

用例生成/草稿保存/资产跳转不回归

前端 build 可过

后端 tests 可过

这是第一步，必须先做
阶段 1：导航、主控台、统一页面框架

目标：让整个平台先“顺手”。

做什么

重做主控台

顶部标签改成“最近访问 / 收藏 / 常用”

统一搜索栏、表格、抽屉、详情布局

系统管理定一套唯一真源

清理模板残留页面入口

验收

首页变成测试工作台

顶部不再堆满页面标签

同类列表页操作风格统一

阶段 2：测试设计闭环补全

目标：把最值钱的资产层站稳。

做什么

需求资产支持更新已有 / 新建版本

测试用例支持真正编辑

增加审核意见、审核历史

增加唯一性/去重策略

增加需求-用例覆盖视图

增加执行结果回链入口

验收

同一需求不会无限复制脏数据

用例能维护，不只是能查

自动化草稿来源可追踪

阶段 3：接口自动化 / Web / App 工作台重构

目标：把三大自动化模块从巨石页拆成工作台。

做法

不要三块一起动，按顺序来：

接口自动化

Web 管理

App 管理

每块都按：

树导航

编辑区

结果区

详情抽屉

快捷定位

来重构。

验收

每个模块页面代码显著变短

保存/运行/结果不再全塞一页

自动化草稿保存后可精确定位

阶段 4：云真机 + 任务管理 + 结果中心

目标：让执行和资源真正闭环。

做什么

设备会话化

任务向导化

结果中心统一入口

任务、设备、结果互相可跳

验收

从任务可跳结果

从结果可跳资产

从设备使用记录可跳会话详情

阶段 5：系统管理与 AI 治理化

目标：让平台长期可维护。

做什么

Prompt 模板管理

AI 调用日志

AI 失败审计

AI 基础统计

用户/角色/菜单真正一体化

验收

你能回答“最近谁在用 AI、哪里失败最多、哪个模式最稳”

权限菜单不再靠前端假数据

系统管理只剩一套真逻辑

六、你这种“AI 直接写代码”的方式，最适合怎么推进

你不懂代码，所以最怕的是：

一次让 AI 改太多

改完不知道有没有伤到别的地方

AI 自己把目录和命名越改越乱

所以你后面每次都要按“任务单”给 AI，而不是一句“重构整个项目”。

给 AI 的任务单模板

你后面每一步都用这个格式发：

1. 目标

这一步要解决什么问题

2. 改动范围

只允许改哪些文件、哪些模块

3. 不允许动的地方

哪些流程绝不能回归

4. 输出要求

前端、后端、文档、测试分别要产出什么

5. 验收标准

改完后怎么判断成功

第一批最适合直接让 AI 开干的任务
任务 1：底座止血

范围：

登录

鉴权

storage/auth/axios

路由重复

env 配置

不要动：

测试设计页面

API/Web/App 主流程

任务 2：主控台改版

范围：

home

count

顶部快捷入口

dashboard 聚合接口

不要动：

侧边栏模块结构

测试设计链路

任务 3：测试设计闭环增强

范围：

requirement

testcase

automation_draft

对应 API 和 model

不要动：

API/Web/App 执行器逻辑

任务 4：先重构接口自动化，不动 Web/App

范围：

api_view

api/api_api

views/api/*

不要动：

Web/App 页面

设备页

七、我的最终建议

你现在最正确的方向不是“继续加菜单”，而是做三件事：

第一，先把底座和安全止血。
第二，把测试设计真正做成资产中心。
第三，再把 API / Web / App / 云真机做成真正的工作台。

你这份最新代码已经有很好的基础了，尤其是 docs/specs 里 M2、M3、M4 的方向其实已经很清楚。
你现在缺的不是想法，而是按阶段收口执行。