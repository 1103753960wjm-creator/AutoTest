# 更新日志 (Changelog)

所有该项目的显著更改都将记录在此文件中。

## [1.1.29] - 2026-07-10

### P0.2 剩余实时连接与接口错误结构收尾

- App 自动化测试用例执行进度从页面内直接 `new WebSocket` 迁到 `frontend/src/composables/useWebSocket.js`，保留有限重连失败后降级轮询。
- 接口测试工作区 WebSocket 调试连接迁到统一 `useWebSocket`，保留手动连接 / 断开、消息历史和发送能力。
- 修正统一 `useWebSocket` 初次连接触发关闭回调的隐患，避免连接时多写“已关闭”消息。
- 接口测试 `move-collection` 的未选目标集合、跨项目移动、无权限移动 3 个错误分支接入 `apps/core/responses.py`，返回 `code/message/error/details/request_id`，成功响应不变。
- 验证结果：后端编译、`manage.py check`、APIClient 400 错误结构请求级验证、临时数据清理检查、前端构建、源码直连扫描、构建产物目标敏感日志扫描、`rule_check.ps1` 和 `git diff --check` 通过。真实浏览器回归按用户要求跳过，不能记为 V4。

## [1.1.28] - 2026-07-10

### P0.2 实时连接和错误结构试点

- 新增 `useEventSource` 和 `useWebSocket` 统一连接入口，先完成需求分析生成进度 SSE 迁移；App 自动化和接口测试工作区实时连接留到后续批次。
- 需求分析生成页不再直接 `new EventSource`，删除旧 `startStreamingProgress`、旧轮询方法和相关调试日志，统一通过 tracker 接收进度和流式内容。
- 新增 `frontend/src/utils/errorMessage.js` 统一解析错误信息，并接入公共 API 拦截器、登录页和需求分析页。
- 新增 `apps/core/responses.py` 错误响应 helper；需求分析生成任务的 400 校验错误、进度 403/404/500、SSE 403/404 JSON 错误完成 `code/message/error/details/request_id` 结构试点，成功响应保持不变。
- 验证结果：后端编译、`manage.py check`、前端构建、APIClient 400/404 错误结构抽样、需求分析页旧 SSE / console 扫描、构建产物 console 扫描、`rule_check.ps1` 和 `git diff --check` 通过；真实浏览器 Network 连接关闭验证未执行。

## [1.1.27] - 2026-07-10

### P0.2 跨模块硬化第一批

- 完成生产配置复核补强：生产环境阻断默认开发 `SECRET_KEY`、`.env.example` 示例密钥、`your-secret-key` 和长度小于 32 的弱密钥。
- 补充 `.env.example` 生产配置说明，明确 `DEBUG`、`ALLOWED_HOSTS`、`CORS_ALLOWED_ORIGINS`、`DB_HOST`、`REDIS_URL` 和敏感信息处理要求。
- 新增 `apps/core/security.py` 脱敏工具，AI 模型调用日志不再输出完整响应正文、API Key、token、Authorization、Cookie 或 password。
- 清理登录、token 刷新、路由守卫、AI 模型配置、Prompt 配置、生成配置和 AI 智能模式配置页的敏感调试 `console` 输出。
- 新增 P0.2 第一批 VDD：`docs/tasks/2026-07-10-p0-2-cross-module-hardening/vdd.md`。验证结果：后端编译、开发 / 生产配置检查、非法生产配置阻断、脱敏抽样、前端构建、规则检查和 diff 检查通过；用户要求跳过浏览器回归，因此真实浏览器回归仍作为残余风险。

## [1.1.26] - 2026-07-09

### AI 需求分析生成用例结果处理闭环修复

- 对比原生系统后补齐当前项目缺失的结果处理承接层：`TaskDetail` 继续作为任务详情和入口页，`GeneratedTestCaseList` 新增“处理结果”抽屉承接生成结果行级处理。
- 结果处理抽屉支持查看生成结果、只选择待处理 `pending` 行、单条 / 多选 / 全部采纳、单条 / 多选 / 全部不通过 / 弃用、编辑生成结果后保存，并在已采纳结果上提供正式资产入口。
- 生成完成页主按钮从旧“保存到用例记录”改为“采纳全部待处理结果”，补齐 loading、防重复提交、空数据检查和项目归属检查，避免旧 `/save_to_records/` 心智绕开当前 `pending/adopted/discarded` 状态模型。
- 新增需求分析结果处理 API 封装，页面新增动作统一调用 `frontend/src/api/requirement-analysis.js`，不再在页面内散写本轮新增端点。
- 新增 `docs/tasks/2026-07-09-ai-generated-result-processing-fix/` 的 Spec/SDD、TDD 和 VDD。验证结果：`rule_check.ps1`、前端 `npm run build`、`git diff --check`、整页跳转扫描和新增端点散写扫描通过；真实浏览器和真实后端任务接口流待补。

## [1.1.25] - 2026-07-08

### P0.1 剩余闭环收口

- 完成剩余 P0.1 的认证一致性和模拟实现清理：注册接口不再返回 `temp_token_*`，统一返回 JWT `access/refresh`；`logout/profile` 显式要求登录态。
- UI 自动化元素定位器验证不再返回模拟成功，真实浏览器验证未接入时返回 501，并让前端展示后端明确错误原因。
- 删除 UI 自动化中未调用的模拟执行日志和模拟失败截图 helper，避免伪造执行证据。
- 接口自动化 Allure 结果时间改为基于执行记录和请求耗时推导，不再使用模拟开始 / 结束时间。
- 页面树“页面名称编辑”未接入保存接口时会明确提示当前不会修改名称，不再静默假成功。
- 新增 `docs/tasks/2026-07-04-p0-1-remaining-closure/` 的公开接口白名单和 VDD。
- 验证结果：后端 `py_compile`、前端 `npm run build`、`rule_check.ps1`、`git diff --check`、DRF 请求级认证验证通过；仍需补真实浏览器主流程、真实 App 报告文件 200、真实无权限对象 403 和浏览器 EventSource 凭据行为验证。

## [1.1.24] - 2026-07-04

### Git 提交信息规则

- 新增 Git / GitHub 提交规范：提交信息必须使用中文总结当前提交内容，禁止只写 `update`、`fix`、`chore`、`wip` 等无法表达提交范围的空泛信息。
- 在根 `AGENTS.md`、`.cursor/workflow_rules.md` 和 `.cursor/project_rules.md` 中同步该规则，后续提交和推送必须先确认暂存范围并排除临时产物。

## [1.1.23] - 2026-07-04

### 开发环境页面切换刷新体感修复

- 修复本地 `npm run dev` 下首次点击懒加载页面时出现卡顿和整页刷新体感的问题。
- 根因是 `frontend/vite.config.js` 长期开启 `optimizeDeps.force: true`，配合 Element Plus 按需样式和大量懒加载页面，首次进入页面时触发 Vite 依赖重优化，出现 `504 Outdated Optimize Dep` 和动态 import 失败。
- 调整 `optimizeDeps`：去掉长期强制重优化，增加页面源码扫描入口，并预优化 Element Plus 深层样式依赖。
- 修复双击 `start.bat` 后前端先启动、后端未 ready 导致 `/api/auth/login/` 代理到 `127.0.0.1:8000` 时 `ECONNREFUSED` 的问题；脚本现在会等待后端端口可连接后再启动前端。
- 已用 Playwright 验证接口自动化、Web 自动化、App 自动化 11 次导航无 `beforeunload/pagehide` 和无 `504 Outdated Optimize Dep`；前端生产构建通过。

## [1.1.22] - 2026-07-03

### P0.1 前后端与样式上线阻断项第一批

- 新增 `frontend/src/assets/css/design-tokens.scss`，并让全局样式、页面壳和共享筛选 / 表格基座开始使用 `--th-*` 设计令牌。
- 前端请求入口收口：数据工厂、数据工厂选择器和执行详情页不再直接使用裸 `axios` 请求，统一走 `frontend/src/api/* -> frontend/src/utils/api.js`；`main.js` 不再配置全局 axios defaults。
- 后端权限补口：需求分析配置检查、生成任务、任务进度、SSE 进度和 App 自动化报告入口不再匿名放开；App 报告文件访问增加执行人 / 项目负责人 / 项目成员 / 管理员对象权限和路径越界校验。
- 根路由不再暴露无对象权限的 App 自动化报告静态目录；开发态 `/media/app-automation/allure-reports/...` 也返回 403，正式访问必须走报告 API。
- 新增 `docs/tasks/2026-07-03-p0-1-full-stack-baseline/` 任务文档，包含 Spec/SDD、TDD、Loop 合同、公开接口白名单和 VDD。
- 验证结果：前端构建、后端编译、规则检查、静态扫描和 DRF 请求级验证通过；仍需补真实浏览器主流程、App 报告真实文件 200、真实无权限对象 403 和浏览器 EventSource 凭据行为验证。

## [1.1.21] - 2026-07-02

### docs 文档目录整理

- 将 `docs` 根目录下散落的 Markdown 文档按主题移动到 `overview`、`architecture`、`ai`、`api`、`api-automation`、`data-factory`、`guides`、`operations`、`planning` 等分类目录。
- 新增 `docs/README.md` 作为文档总入口，明确每个分类目录的职责和新增文档放置规则。
- 新增 `docs/guides/AI开发规范迁移复用提示词.md`，沉淀新项目初始化规范提示词、项目稳定后二次优化提示词和遗漏检查清单，方便后续跨项目复用。
- 将接口自动化 P0 归档目录移动到 `docs/api-automation/archive/p0-docs-2026-06-18/`。
- 将 Loop 合同模板收敛到 `docs/task-templates/loop-contract-template.md`。
- 补齐历史缺失引用入口：`docs/architecture/unified-table-template-spec.md` 和 `docs/guides/frontend-ui-style-guide.md`。
- 更新文档和项目记忆中的旧路径引用，修复数据工厂与 APP 历史文档中的明显错误相对路径。
- 新增 `docs/tasks/2026-07-02-docs-reorganization/`，按模板记录本轮整理的 Spec/SDD、TDD 和 VDD。

## [1.1.20] - 2026-07-02

### Harness 与受控 Loop 规则增强

- 新增 `.cursor/autonomy_rules.md`，定义 AI 自治等级 L0-L4、决策权限矩阵、用户已有改动保护、UTF-8 读取硬规则和规则冲突处理。
- 新增 `.cursor/loop_rules.md`，定义受控 Loop 的进入条件、Loop 合同、变更预算、失败处理、验证等级、停止条件和运行日志要求。
- 新增 `docs/task-templates/loop-contract-template.md`、`docs/project-memory/loop_run_log.md` 和 `docs/project-memory/development_checklist.md`，让 Loop 目标、过程、验证和暂停原因可审计。
- 新增 `scripts/rule_check.ps1`，先覆盖 P0 高频红线扫描，包括整页刷新、旧接口路径、`xlsx`、页面直连 `@/utils/api`、缺失 `@submit.prevent`、原生按钮缺少 `type` 和假入口文案。
- 更新 `AGENTS.md`、`.cursor/prompt.md`、`.cursor/workflow_rules.md` 和 `.cursor/project_rules.md`，明确开发前必须判断自治等级、是否允许受控 Loop，并把 `Spec/SDD`、`TDD`、`VDD`、需求确认和交付说明统一要求为中文大白话、细颗粒度表达。
- 明确用户默认是中国国内的软件测试工程师，规则、文档和说明必须便于用测试思维判断范围、风险、验收和异常场景；读取和写入中文文件必须保持 UTF-8。
- 新增 `docs/task-templates/` 与 `docs/tasks/README.md`，将 `Spec/SDD`、`TDD`、`VDD` 模板化，限制必须包含标题、背景、范围、方法、验证内容、是否通过验证、未验证项、残余风险和回退止损等字段。

## [1.1.19] - 2026-07-02

### 开发规范错误记录机制增强

- 新增 `docs/project-memory/error_event_log.md`，用于第一时间记录命令失败、构建失败、接口异常、页面报错、控制台红错、验证失败、环境阻塞和规则执行偏差。
- 调整根 `AGENTS.md` 读取顺序，开发前优先读取错误事件日志与错误防复发手册。
- 在 `.cursor/workflow_rules.md` 增加“错误记录闸门”：开发前读取、开发中遇错先记、收尾判断是否升级到防复发手册。
- 收口 `error_prevention_log.md` 职责，明确其只沉淀已确认根因、具备复发风险并能形成防复发规则的错误模式，避免与现场事件日志混写。

## [1.1.18] - 2026-06-19

### 接口自动化阶段 A 对象闭环 P0 补强

- **测试套件正式入口拆分**：接口自动化侧边栏新增 `/api-testing/test-suites`“测试套件”正式入口，旧 `/api-testing/automation` 改为隐藏兼容重定向；同步 route meta、Dashboard 快捷入口和中英文文案，避免只改页面不改导航真源的半拆分状态。
- **接口测试用例固定字段补齐**：`/api-testing/test-cases` 表格补齐所属项目、所属集合筛选、来源和最近执行状态；最近执行状态由后端列表接口聚合最近一条 `RequestHistory` 返回，避免前端逐行请求历史。
- **接口测试用例移动集合闭环**：新增 `POST /api-testing/requests/{id}/move-collection/`，列表页提供“移动集合”弹窗，后端校验同项目和项目权限。
- **请求历史清空闭环**：新增 `POST /api-testing/histories/clear/`，按当前筛选范围真实清空历史，前端补二次确认、loading、成功刷新和失败提示。
- **套件级断言闭环**：新增 `POST /api-testing/test-suite-requests/{id}/assertions/`，测试套件页提供居中编辑弹窗并保存 `TestSuiteRequest.assertions`；套件执行优先使用套件级断言，空时回退接口自身断言。
- **接口用例归属口径收口**：隐藏调试工作区可查看和修改所属集合，保存时必须选择集合；新建入口不再继续制造新的无集合归属用例。
- **高风险提示与契约修正**：项目负责人字段改为只读展示；删除项目、集合、接口测试用例、环境前补充级联风险提示。
- **请求封装收口**：接口自动化页面业务请求继续收口到 `frontend/src/api/api-testing.js`，避免页面直接导入 `@/utils/api`。
- **文档与记忆同步**：新增 `docs/api-automation/api-automation-p0-object-closure-fix-vdd.md`，同步归档 README、项目记忆、决策日志、模块记忆、错误模式库和任务交接。
- **验证结果**：后端 `py_compile` 和前端 `cmd /c npm run build` 通过；静态检索未发现新增整页刷新兜底、P0 假入口或接口自动化页面直接业务请求。构建仍保留既有 `web-tree-sitter` 警告。

## [1.1.17] - 2026-06-19

### 接口自动化阶段 A 对象闭环 TDD 确认项

- 更新 `docs/api-automation/api-automation-p0-object-closure-fix-tdd.md`，确认请求历史“清空历史”入口必须真实实现，不移除、不保留假提示。
- 确认套件级断言入口必须实现编辑弹窗并保存 `TestSuiteRequest.assertions`，弹窗样式参考当前系统已有编辑弹窗。
- 同步更新归档目录 README 和任务交接文档，强调阶段 A 仍需最终确认后才能进入代码实现。

## [1.1.16] - 2026-06-18

### 接口自动化 P0-2 AI 生成目标类型规格补强

- 更新 `docs/api-automation/api-automation-p0-2-ai-target-type-spec.md`，补充接口测试用例与测试套件的对象边界：接口测试用例由 `ApiRequest` 承接，测试套件由 `TestSuite + TestSuiteRequest` 承接。
- 明确 AI 生成 `api_test_case` 时只输出 `ApiRequest` 兼容字段，不直接生成或修改测试套件，后续是否加入套件由接口测试用例列表或套件页显式操作。
- 冻结 P0-2 红线：当前 AI 生成测试用例逻辑严禁重写，只允许套用 `target_type`、Prompt 按类型选择、任务固化和结果字段展示契约。
- 同步更新 `docs/api-automation/api-automation-p0-2-ai-target-type-tdd.md`、项目记忆、决策日志、模块记忆、错误预防库和对话启动摘要，强调进入代码实现前仍需先确认 Spec，再确认 TDD。

### 接口自动化对象级闭环审计

- 新增 `docs/api-automation/api-automation-object-closure-audit.md`，按对象操作粒度审计接口自动化模块的承接点、页面入口、按钮控件、后端接口、结果回流和异常反馈。
- 明确接口测试用例移动集合、集合父级编辑、套件级断言、请求历史清空、项目负责人字段契约等当前断点。
- 汇总 P0-P3 阶段建议修复范围，优先将“用户已看到入口但操作不闭环”的问题纳入 P0 修复。

## [1.1.15] - 2026-06-17

### 接口自动化 P0-1 用例闭环

- **新增接口测试用例正式入口**：接口自动化侧边栏新增 `/api-testing/test-cases`，旧 `/api-testing/interfaces` 隐藏兼容重定向，不直接 404。
- **接口测试用例产品化收口**：`/api-testing/test-cases` 改为真实资产列表页，支持查询、分页、新建、编辑/调试、执行、加入同项目测试套件、指定用例历史和删除；旧调试树仅保留为隐藏工作区 `/api-testing/test-cases/workspace?caseId={id}&projectId={projectId}`。
- **补齐接口测试用例列表交互细节**：筛选区“查询 / 重置”按钮增加上间距，避免贴住筛选框；隐藏调试工作区新增“返回用例列表”按钮，避免用户进入编辑/调试后只能依赖浏览器返回。
- **优化加入测试套件反馈闭环**：加入套件操作按钮停止冒泡，套件加载中和无套件时禁用确认按钮；未归属集合、无可用套件、套件加载失败、未选择套件、加入成功和加入失败统一使用 `ElMessageBox` 弹窗反馈，不再出现无反应或仅在页面底部弱提示。
- **修复服务式弹窗样式缺失**：前端入口显式引入 Element Plus `loading`、`message`、`message-box` 服务组件样式，恢复 `ElMessageBox` 遮罩、居中、按钮和弹窗官方样式，避免服务式弹窗裸 HTML 或位置异常。
- **修复隐藏工作区深链切换**：列表进入工作区时携带 `projectId`，工作区监听 `caseId/projectId` 变化并丢弃旧详情响应，快速连续打开不同用例时最终停在最后点击目标。
- **冻结 P0-1 技术载体**：不新增 `ApiTestCase` 表，继续以 `ApiRequest` 承接接口测试用例、断言和调试执行。
- **修正前端旧接口路径**：单接口执行统一走 `/api-testing/requests/{id}/execute/`，执行结果详情统一走 `/api-testing/test-executions/{id}/`。
- **请求封装收口**：`InterfaceManagement.vue` 不再直接导入 `@/utils/api`，接口测试用例工作区请求统一经由 `frontend/src/api/api-testing.js`。
- **补齐断言结果落库与展示**：单接口执行成功后 `RequestHistory.assertions_results` 真实写入数据库，请求历史详情新增“断言结果”页签展示通过/失败、类型、期望、实际和错误。
- **修复平台壳快速导航卡住**：侧边栏去掉 `pointerdown/click` 重复导航触发，只保留 `el-menu @select`；导航调度改为最后一次点击优先；浏览器标题写入增加当前真实路由校验，避免跨顶部模块快速切换时页面、URL、标题和侧栏不一致。
- **硬化 SPA 表单默认行为**：全局 Vue 表单补齐 `<form>` / `<el-form>` 的 `@submit.prevent`，原生 `<button>` 显式声明 `type`，避免 Enter 键或默认 submit 绕开 Vue Router 触发当前 URL 重新请求，造成卡住或整页刷新体感。
- **验证结果**：`git diff --check`、后端 `py_compile`、旧路径静态检索和 `cd frontend && cmd /c npm run build` 均通过；构建仍保留既有 `web-tree-sitter` 警告。

## [1.1.14] - 2026-06-16

### 前端非 P0 改动质量收口

- **恢复列表页筛选区组件契约**：将测试用例列表与版本列表的筛选区收回 `ListShell` 的 `#filters` 插槽，避免筛选栏落入内容卡片造成页面结构不一致。
- **统一认证失效跳转入口**：新增 `authNavigation` 工具，`userStore` 与 `api` 拦截器不再各自维护登录跳转逻辑；由 `router/index.js` 注册 router 实例，避免继续制造 router 动态/静态混合导入警告。
- **修复顶部大模块与侧边栏连续切换卡顿**：`App.vue` 根层改为登录后统一 Layout key，跨顶部大模块时不再销毁整个平台壳；顶部模块、侧边栏、全局搜索、最近访问、收藏和个人资料入口统一走导航调度器，快速连点时最终停在最后点击目标。
- **同步项目文档与记忆**：将导航稳定性、认证跳转、生产配置安全、Excel 导出依赖替换、页面头部 actions 容错等结论同步到长期规则、前后端本地规则、项目记忆、错误预防日志、页面规范和烟雾验证基线。
- **清理未跟踪一次性脚本产物**：移除 `fix_filters.py`、`fix_malformed.py`、`fix_spans.py`、`scan_filters.py`、`generate_api_doc.py`、`schema.json`、`schema2.json` 等临时文件，避免误入仓库。
- **验证结果**：`npm run build`、`node --check` 和 `git diff --check` 均通过；此前 router 动态/静态混合导入警告已消失，仍保留既有 `web-tree-sitter` 的 `fs/path` 浏览器兼容与 `eval` 警告。

## [1.1.13] - 2026-06-14

### 🔒 P0 安全与依赖收口
- **收紧后端启动配置校验**：`DEBUG` 与 `DISABLE_CSRF_FOR_API` 改为项目级严格布尔解析，非法值会明确提示配置项和允许取值。
- **移除生产环境危险兜底**：生产环境必须显式配置 `ALLOWED_HOSTS` 与 `CORS_ALLOWED_ORIGINS`，不再默认放开全部来源；生产环境禁止开启 API CSRF 全局禁用。
- **替换高危 Excel 导出依赖**：移除无修复版本的 `xlsx`，新增 `write-excel-file` 与统一 `excelExport` 工具，测试用例列表、需求分析页和任务详情页导出功能改走新封装。
- **验证结果**：后端开发/生产合法配置 `manage.py check` 通过，非法 `DEBUG`、生产缺失 CORS、生产开启 API CSRF 禁用均按预期失败；前端 `npm run build` 通过，`npm audit --omit=dev` 为 0 漏洞。
- **残余说明**：前端构建仍保留既有 `web-tree-sitter` 浏览器兼容与 `eval` 告警；路由混合导入告警已在后续认证导航收口中消失。

## [1.1.12] - 2026-06-01

### 🐛 测试设计导航与筛选样式缺陷修复
- **消除评审列表控制台缺失翻译告警**：补齐 `reviewList.helperText`、`reviewList.totalTasks`、`reviewList.viewAutoReviews` 中英文文案，避免侧边栏进入评审列表时输出 i18n missing warning。
- **移除需求分析页显式整页刷新点**：`resetGeneration` 不再调用 `window.location.reload()`，改为组件内重新检查配置状态，避免平台壳被强制重载。
- **统一测试设计列表筛选区样式**：版本管理、测试用例列表改为与评审列表一致的 `.filters` 三列筛选结构；`ListShell` 同步补充筛选行与输入控件撑满规则，防止 flex 容器下宽度塌缩。
- **验证结果**：`frontend` 执行 `cmd /c npm run build` 通过；仍保留既有 `web-tree-sitter` 浏览器兼容与 `eval` 构建告警。

## [1.1.11] - 2026-05-31

### 🐛 平台 UI/UX 四项 Bug 修复
- **完成全局页面体验与一致性审计**：全面推广了侧边栏切换修复、滚动条恢复和筛选控件样式对齐策略。
- **统一全局所有 ListShell 筛选器样式**：通过自动化脚本检索了全仓 18 个模块列表页，将 `.filters` 内的 `el-col` 统一规范为 `span="8"`，并为全部 `el-input`、`el-select`、`el-date-picker` 等筛选组件强制注入 `style="width: 100%"`，彻底消除了 flex 容器下跨模块宽度塌缩和排列错乱的缺陷。
- **修复全局导航失灵**：`router-view` 的 `:key` 从 `currentRoute.fullPath` 改为 `currentRoute.name || currentRoute.path`，避免同一路由 query 变化导致 keep-alive 下组件不断销毁重建、堆积后事件绑定丢失的问题。
- **修复需求分析页和 AI 生成用例页无法滚动**：`platform-route-wrapper` 移除 `height: 100%; overflow: hidden` 内联样式，改为 `min-height: 0`，让子页面内容高度正确传递到外层 `.platform-layout__main` 的 `overflow: auto` 滚动容器。
- 同步更新 `error_prevention_log.md`、`module_memory.md`、`task_handoff.md` 项目记忆。

## [1.1.10] - 2026-05-30

### 🛠 平台稳定性与架构底层修复
- **彻底修复侧边栏切换崩溃 (Vue 3 渲染上下文丢失 Bug)**：由于 `<router-view>` 插槽内缓存容器层级被 `v-if` 打断，引发了 Element Plus 表格与表单卸载时抛出 `reading 'exposed' / 'parentNode'` 的致命异常。通过将路由容器结构提至缓存外部，移除了 `v-if` 条件渲染干扰，从架构层面完美避开了组件卸载时的空指针异常。
- **修复列表组件 Props 类型验证崩溃**：排查 `UnifiedListTable.vue` 爆出的 `TypeError: Cannot convert object to primitive value`，移除了 `type` 数组中非法的 `null` 构造器类型，修复了通用列表组件由于无效 Prop 类型声明引发的白屏与警告。
- **清理闲置引用并消除 Linter 报错**：移除了 `ProjectList.vue` 中导入但未使用的 `FilterBar` 变量，恢复了正常热更新。
- **登录网络异常溯源与排查**：对 `POST /api/auth/login/ 404 (Not Found)` 的问题进行了完整网络链路溯源，确认了 Vite 配置与 Django 路由映射代码完全正确。此 404 伪报错明确为本地后端服务脱落或拦截器阻断引发的环境异常。
- **项目记忆与防复发手册同步**：在 `error_prevention_log.md` 补充 008 错误模式，在 `decision_log.md` 新增 Vue 3 路由缓存架构禁止在容器层混用 v-if 决策，同步重写最新的 `task_handoff.md`。

## [1.1.9] - 2026-05-26

### ✨ AI自动化执行记录页重构迁移与 UI 一致性规范建设
- **前端样式 UI 一致化规范落地**：在 `.cursor/project_rules.md` 与 `frontend/AGENTS.md` 中正式写入一致性排版红线，规范所有模块的搜索筛选栏、查询/重置按钮必须在排版、位置和网格间距上与 `ProjectManagement.vue` 等标准页面保持完全一致，杜绝 ad-hoc 散写。
- **页面组件与状态收口**：将 `AIExecutionRecords.vue` 迁移至 `ListShell` 统一页面壳和 `UnifiedListTable` 统一表格组件，移除历史陈旧 CSS；接入 Loading、Forbidden、Error、Empty、SearchEmpty 五种标准状态提示。
- **用例检索与重置闭环**：添加“用例名称”检索框，配合 Unified 表格及 `StateSearchEmpty` 状态流转，实现查询和一键重置全量数据的高效交互。
- **静默轮询机制健壮性优化**：定时轮询自动携带当前搜索条件，防止视图闪烁或重置；维持勾选记录时暂缓轮询刷新的机制，避免列表覆盖阻断批量删除操作。
- **表格与分页通用范本建设**：编写并沉淀了全新的 `docs/architecture/unified-table-template-spec.md` 规范，详细阐述了基于 Vue 3 Script Setup 的通用表格、检索、分页、批量操作与递归 Excel 导出的标准脚手架模板，并在项目规则与启动记忆中挂接该真源索引，为未来新页面开发及新项目复用提供了清晰的依据。

## [1.1.8] - 2026-04-20

### 前端打包体积优化：消除大包体积告警
- `vite` 构建配置补充了更明确的手动拆包策略，将框架基础包、组件库、图表库、Excel 处理和 cURL 转换工具链拆成独立包，降低主包体积压力。
- 前端入口不再整包挂载 Element Plus，改为按页面自动引入组件，仅保留全局 loading 指令和图标注册，减少首屏基础包负担。
- 报表相关页面的图表库改为按需引入，只保留当前页面真实使用的图表、组件和渲染器，避免整包图表能力进入公共包。
- 继续收紧路由级懒加载，降低管理台首页和主链页面首屏时一起打入的大体积代码。
- 前端重新构建后已不再出现“大包体积”告警；当前仍保留 `curlconverter / web-tree-sitter` 依赖链带来的浏览器兼容与 `eval` 告警，本轮未处理这部分实现路线。

## [1.1.7] - 2026-04-19

### 任务详情页动作归位：主按钮统一进入页头动作区
- `TaskDetail` 页头动作区正式接管“返回、查看项目测试用例、进入结果批次页、取消生成、导出 Excel”等主按钮，内容区不再单独摆放一排主动作。
- 删除任务详情页内容区中重复出现的“进入结果批次页”按钮，结果处理说明卡片和结果预览区改为只保留说明文案。
- 将“页面主动作没有统一进页头动作区，导致按钮乱放和重复入口”沉淀到 `docs/project-memory/error_prevention_log.md`，作为后续页面收口的防复发规则。

## [1.1.6] - 2026-04-19

### 2.2 第二阶段继续收口：任务页只保留预览和入口
- `TaskDetail` 继续收紧为任务对象页，结果详情弹窗改为只读预览，不再在任务页内继续承接结果编辑、采纳或弃用动作。
- 任务页结果弹窗底部统一改为“前往结果批次页 / 查看正式资产 / 关闭”三类动作，进一步明确结果处理应在结果批次页完成。
- 同步删除任务页中已不再使用的结果处理旧逻辑，减少后续维护时再次把任务页误用为结果处理主战场的风险。

## [1.1.5] - 2026-04-19

### Harness Engineering：错误模式库与防复发手册接入
- 新增 `docs/project-memory/error_prevention_log.md`，作为正式项目记忆的一部分，用于沉淀已证明会重复出现的错误模式、根因分析、防复发规则与最低验证动作。
- 在 `AGENTS.md` 与 `.cursor/workflow_rules.md` 中补充错误模式库的读取顺序与回写规则，明确“同类错误第二次出现”或“已可稳定归纳出防复发规则”时应写入错误模式库。
- 在 `module_memory.md`、`task_handoff.md`、`dialogue_bootstrap.md` 中同步挂接错误模式库，让模块经验、最近交接与新会话启动都能直接指向这份防复发手册。

## [1.1.4] - 2026-04-16

### 页面文案正式化统一：清理阶段性与开发口径提示
- 统一修订需求分析、结果批次、执行详情、项目详情、自动评审列表以及各模块 Dashboard 中的页面说明文案，去除“本轮、收口、预留、主战场、不伪装”等开发过程表述。
- 同步调整任务详情页、结果批次页、需求分析页与配置页的兜底文案，以及 `apps/requirement_analysis/serializers.py` 返回的摘要说明，确保接口返回与页面展示口径一致。
- 统一将侧边栏与导航中的“预留”状态改为更正式的“规划中 / 待接入 / 待补充”等表达，减少用户对占位能力和阶段性实现的误解。
- 优化 API、UI、APP 自动化 Dashboard 中风险提醒与说明信息的表述方式，避免在首页展示带有实现细节或阶段性设计痕迹的提示。
- 补齐“任务详情 -> 结果批次页 -> 正式资产页”主链的页面级回跳能力：结果批次页和正式资产页新增来源返回动作，任务详情页直达正式资产页时同步补传 `taskId` 上下文。

## [1.1.3] - 2026-04-15

### 2.2 主链闭环修复：任务、结果批次与正式资产口径对齐
- `GeneratedTestCaseList` 在通过 `taskId` 进入时改为真实按来源任务收口结果批次数据与统计口径，不再只是顶部文案提示“当前焦点任务”。
- `TaskDetail` 继续收紧为任务对象页，结果预览区保留状态展示与入口说明，批量采纳、弃用等结果处理主动作从任务页 UI 中退出，统一引导到结果批次页处理。
- 正式测试资产列表页明确 `sourceTaskId` 仅作为来源上下文提示，不再制造“正在按来源任务过滤正式资产”的假象，保持正式资产层和结果层的边界一致。
- `apps/requirement_analysis/serializers.py` 中的下游摘要文案同步改为“收口到结果批次页”，对齐当前前后端真实职责表达。

## [1.1.2] - 2026-04-15

### 上下文管理与项目记忆体系增强
- 在 `docs/project-memory/` 下新增 `decision_log.md`、`module_memory.md`、`task_handoff.md`，把项目记忆从“阶段记忆 + 启动记忆”升级为“阶段、决策、模块、交接”四类正式记忆。
- 修正 `docs/project-memory/current_phase.md` 与 `docs/project-memory/dialogue_bootstrap.md` 中的旧规则引用，统一对齐到当前 `AGENTS.md` 与 `.cursor/*.md` 规则体系。
- 在根 `AGENTS.md` 与 `.cursor/workflow_rules.md` 中补充正式记忆文件索引与记忆回写规则，明确阶段变化、决策冻结、模块经验、任务交接分别写回哪份记忆文件。

## [1.1.1] - 2026-04-15

### 规则体系重构：入口化、分层化、目录化
- 将根目录 `AGENTS.md` 收口为仓库入口规则，明确“读取顺序、流程闸门、红线边界、验证入口、子目录路由”五类信息，不再继续膨胀为细节堆叠文件。
- 重构 `.cursor/prompt.md`、`.cursor/workflow_rules.md`、`.cursor/architecture.md`、`.cursor/storage_rules.md`、`.cursor/project_rules.md`，改为更清晰的“文件职责 + 决策边界 + 最低验证 + 明确禁止”结构，便于 AI 在新任务中快速判定该遵守什么。
- 新增 `frontend/AGENTS.md`、`backend/AGENTS.md`、`apps/requirement_analysis/AGENTS.md` 三个目录级本地规则文件，让高频高风险区域可以在不污染全局规则的前提下做局部加严。
- 结合当前项目真实问题，把“请求重复触发、超大分页全量拉取、轮询收口、日志去重、结果对象与任务对象边界、AI 接入统一入口”等经验正式写入规则层，减少后续 AI 开发走偏和返工成本。

## [1.1.0] - 2026-04-03

### 2.2 第二阶段：结果层与正式资产层继续收口
- `GeneratedTestCaseList` 继续强化为“结果批次页 / 结果处理页”，补充来源任务焦点语义，并新增从结果批次直达正式测试资产层的轻量入口。
- `TaskDetail` 继续收紧为“任务对象页”，明确结果批次页才是结果层主入口，任务页只保留摘要、入口和轻量预览，不再继续膨胀为结果处理主战场。
- `apps/testcases/serializers.py` 为正式测试资产层统一补齐 AI 来源摘要字段中的轻量回链信息，包括来源任务、结果批次和项目列表入口路径占位，同时保持老数据“来源未记录 / AI 来源待补齐”兼容。
- `TestCaseList`、`TestCaseDetail`、`TestCaseEdit` 继续收口为正式测试资产层：统一展示来源摘要、AI 来源位和自动化预留位，并在详情页、编辑页补充轻量回链入口，不越界进入 `2.3` 自动化草稿中心。

## [1.0.9] - 2026-04-03

### 阶段 2 总纲文档与当前真实状态同步
- 同步修订 `docs/overview/阶段2优化文档.md`，新增“基于当前仓库真实状态的同步说明”，明确该文档继续作为阶段 2 总纲文档使用，不替代 `docs/project-memory/current_phase.md`。
- 明确写回当前真实阶段位置：`2.1` 已完成并冻结，`2.2` 已完成第一阶段且已进入第二阶段，当前不再回头重做 `2.1`。
- 将“下一步最合理的动作”修正为继续推进 `2.2 第二阶段`，并把本轮范围固定为“结果层与正式资产层收口”，明确不进入 `2.3 自动化草稿中心`。
- 在 `2.2` 段内补充当前主链、结果层职责、正式资产层职责与本轮非目标，避免阶段文档与 `current_phase`、真实代码状态继续脱节。

## [1.0.8] - 2026-04-02

### ✨ 测试设计列表统一状态与分页稳定性补强
- 测试用例列表与版本列表接入统一页面状态组件，补齐加载、空态、搜索空态、请求失败与无权限展示。
- 在列表请求后补齐“页码越界回退”兜底，避免删除或筛选后分页器落到空页不可用。
- 保持原有筛选、排序、分页、批量删除与双击编辑行为不变。
- 扩展到执行计划列表页和接口通知日志页，统一接入页面状态组件、分页越界回退以及底部分页器布局。
- 第三批主列表页继续推进到 UI 自动化通知日志页、App 自动化通知日志页、UI 自动化执行列表页和 App 自动化执行列表页，统一接入页面状态组件、表格骨架与分页越界回退。
- 继续补齐 UI 自动化报告列表页、API 请求历史页和 API 定时任务页的统一状态、统一表格骨架与分页稳定性；其中 API 请求历史表格组件也切换为复用 `UnifiedListTable`。
- 补齐 App 自动化报告列表页的套件报告与用例报告双列表状态收口，统一接入页面状态组件、分页越界回退与稳定分页器布局。
- 继续推进 App 自动化剩余主列表页，完成设备列表页、应用包管理页、元素列表页、APP 测试用例列表页和测试套件列表页的统一状态接入、统一表格骨架改造与分页稳定性补强。
- 继续推进 API 测试主列表页，完成项目管理页、环境管理页和通知管理页的统一状态接入、统一表格骨架改造与分页稳定性补强；其中环境管理页按全局环境与局部环境两套分页独立收口。
- 首页工作台正式收口到统一 dashboard 壳思路，去掉假待办、假风险和阶段性占位数据，改为以最近访问、收藏入口和核心模块入口作为首页真实能力主轴。
- 接口自动化模块继续向平台统一壳收口，项目管理页、请求历史页、环境管理页和定时任务页改为接入统一页面壳结构，并删除历史遗留的模块内顶栏容器文件。
- 按导航冻结方案收紧接口自动化模块侧边栏，只保留正式页面入口；测试报告、定时任务、通知日志和 API AI 服务配置不再作为当前模块正式导航项，但旧路由仍可直接访问。

## [1.0.7] - 2026-04-01

### 🧭 全局规则升级为统一开发标准
- 将全局 `GEMINI.md` 重构为跨项目可复用的开发标准，统一采用 `Spec/SDD -> TDD -> Execution -> VDD` 流程。
- 在全局规则中上收 `Spec/SDD` 模板、`TDD` 测试设计模板、测试用例模板和 `VDD` 验收清单模板。
- 保留并强调“Spec 阶段有不确定项必须先问用户”“双确认闸门不变”的强约束。
- 将仓库内 `.cursor/workflow_rules.md` 收敛为 TestHub 项目补充规则，避免与全局规则重复维护。
- 修正规则文件中的历史 `.antigravity/*.md` 引用，统一改为当前仓库实际使用的 `.cursor/*.md`。

## [1.0.6] - 2026-03-30

### ✨ AI 生成结果列表表格样式对齐测试设计列表
- 将 AI 生成结果页切换为复用 `UnifiedListTable`，统一对齐测试设计模块测试用例列表的边框、表头、分页和链接型操作样式。
- 按测试设计列表口径重排 AI 生成结果列顺序，补齐独立的“来源项目”“处理状态”“AI 评审状态”展示位。
- 保留原有详情、编辑、删除操作能力，并让多选结果直接走批量删除入口。

## [1.0.5] - 2026-03-29

### ✨ 测试设计模块前端 UI 现代化与批量删除补全
- **后端 API 标准化**：补齐项目管理 (`/projects/batch-delete/`) 和版本管理 (`/versions/batch-delete/`) 的批量删除接口，统一级联删除二次确认 (`confirm`) 的校验规范。
- **前端表格 UI 升级**：统一 `UnifiedListTable` 的视觉风格，包括全量边框、浅色表头背景，以及操作列按钮改为文字链接 (`link`) 形式。
- **交互逻辑闭环**：为项目、版本、用例列表补齐第一列序号列，全量支持多选框批量操作，并统一将行双击动作设为“触发编辑”。
- **版本管理增强**：在版本列表中新增“创建人”和“用例数”字段展示。

## [1.0.4] - 2026-03-26

### 🛠 AI 生成结果批量采纳体验与性能修复
- 任务详情页的“一键采纳”新增提交中状态，点击后会立即显示处理中提示，并临时禁用批量按钮、选择框和相关结果操作，避免用户误判未生效后重复点击。
- 补充任务详情页中的英文文案，统一“采纳中 / 已提交请求 / 自动刷新”提示语义。
- 批量采纳后端新增按 `task_id` 维度的 AI 用例查重缓存，避免每条结果重复全量扫描项目用例。
- 批量采纳链路改为在事务中批处理，并把结果状态快照收敛为循环结束后统一保存，减少循环内重复 `save()` 和默认版本重复解析带来的耗时。

## [1.0.3] - 2026-03-26

### 🔆 AI 生成链路状态收口
- 新增自动 AI 评审记录对象 `TaskAutoReviewRecord`，并补齐统一 AI 自动评审入口页与列表接口。
- 需求分析页新增按项目维度的任务恢复上下文，刷新或切页后可恢复当前生成任务入口与状态感知。
- 生成任务链路引入统一前端 tracker，收口 `SSE / polling / 本地状态`，避免旧页面实例继续收流。
- 取消生成升级为应用层协作式真取消，关键阶段与最终结果写库前都会再次检查取消状态，避免 `cancelled` 被后续写回污染。
- 生成任务详情页开放全状态查看，并按任务状态与结果处理状态双重门禁控制取消、采纳、弃用等操作能力。
- 修复 AI 需求分析 / 编写测试用例在流式阶段因 async 上下文触发同步 ORM 检查而自动失败的问题，并补齐需求分析页失败态错误提示，避免页面静默失败。

## [1.0.2] - 2026-03-25

### 🧭 规则体系整理 (Rules)
- 新增仓库内 `.cursor/` 规则目录，按 `prompt`、`workflow_rules`、`architecture`、`storage_rules`、`project_rules` 五个文件承载项目规则。
- 将根目录 `MEMORY.md` 迁移并更名为 `docs/project-memory/current_phase.md`，明确其职责为“项目开发记忆”。
- 新增 `docs/project-memory/dialogue_bootstrap.md`，作为非正式规则层的“对话启动记忆”。
- 更新 `AGENTS.md` 的读取顺序与仓库基线描述，改为指向仓库内规则层和新的记忆文件路径。
- 精简全局 `GEMINI.md`，移除具体项目基线，改为只保留跨项目通用规则与四层优先级定义。
- 在 `.cursor/workflow_rules.md` 中补充 `Spec` 最小模板、测试设计方法和验收清单模板。

## [1.0.1] - 2026-03-21

### ✨ 新增与优化 (Added & Improved)
- 新增 AI 生成链路规范文档 (`docs/ai/ai-generation-chain-spec.md`)。
- 新增 AI 生成取消规范文档 (`docs/ai/ai-generation-cancel-spec.md`)，同步明确取消行为对前后端的约束。
- 完善需求分析模块：支持后端模型、序列化及视图处理，包含结果解析与状态流转。
- 扩展测试用例模块：新增 AI 来源去重功能 (`ai_source_dedup.py`)，避免重复生成资产。
- 重构前端 AI 链路，在业务链路中深度集成 AI 能力，包括模型配置 (`AIModelConfig.vue`)、Prompt 配置 (`PromptConfig.vue`) 和生成参数管理 (`GenerationConfigView.vue`)。
- 优化前端展示体验：升级需求分析页、生成测试用例列表与项目详情展示页，形成更完整的 AI 生成转化闭环。

## [1.0.0] - 2026-03-19

### ✨ 新增 (Added)
- 初始化 AutoTest 测试平台项目代码库。
- 包含基于 Vue 3 + TypeScript + Element Plus 的前端管理台代码。
- 包含基于 FastAPI + Tortoise ORM 的后端服务端代码。
- 新增各项基础配置文件、启动脚本及开发规范文档。

### 🔵 更新 (Updated)
- 重新定义并细化了阶段 2 的优化路线图文档（`docs/overview/阶段2优化文档.md`）。
- 新增了测试设计资产收口相关规范文档（`docs/architecture/test-design-asset-spec.md`）。
- 同步更新了前端业务页面：需求分析页、用例详情页、用例编辑与列表页、执行详情页等，以支持测试设计资产层与来源的追踪闭环。
